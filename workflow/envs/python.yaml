name: fire
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - tqdm
  - defopt
  - numpy==1.25 # setting exactly due to picky numpy numba compatibility issues
  - numba
  - scipy
  - pandas==1.5
  - pysam>=0.19
  - htslib>=1.17
  - cmake
  - pip
  - pip:
      - polars[pyarrow]>=0.19
