name: fire
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - tqdm
  - defopt
  - numpy==1.23 # use this version
  - numba==0.56.4 # use this version
  - libgcc
  - scipy
  - pandas==1.4 # pandas versions later than this have cxx errors
  - libstdcxx-ng
  - pysam>=0.19
  - htslib>=1.17
  - pip
  - pip:
      - polars[pyarrow]>=0.19
