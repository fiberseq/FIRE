name: fire
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - tqdm
  - defopt
  - numpy==1.24
  - numba
  - libgcc
  - scipy
  - pandas==1.4 # pandas versions later than this have cxx errors
  - pysam>=0.19
  - htslib>=1.17
  - pip
  - pip:
      - polars[pyarrow]>=0.19
