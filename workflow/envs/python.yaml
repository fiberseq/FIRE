name: fire
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - tqdm
  - defopt
  - numpy==1.23 # use this version to avoid cxx errors
  #- numba==0.56.4
  - scipy
  - pandas==1.4 # pandas versions later than this have cxx errors
  - pysam>=0.19
  - htslib>=1.17
  - pip
  - pip:
      - numba # try installing numba with pip to avoid cxx errors?
      - polars[pyarrow]>=0.19
